# Watcher configuration
# https://www.npmjs.com/package/chokidar

globs:
  include:
    - '${projectPath}/services/**'
    - '${projectPath}/lib/**'
    - '${projectPath}/**/*.yml'
  ignore:
    - '**/node_modules/**'
    - '**/vars.json'
    - '**/event.input.json'
