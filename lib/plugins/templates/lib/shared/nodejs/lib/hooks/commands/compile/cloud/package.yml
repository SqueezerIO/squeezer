
- description: Installing packages for function ${func.name}
  bin: cd ${output} && ${project.path}/node_modules/yarn/bin/yarn.js
  args:
    - 'install'
    - '--silent'
    - '--no-progress'
    - '--non-interactive'
    - '--prefer-offline' 