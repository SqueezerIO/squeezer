'use strict';

describe('Squeezer CLI integration tests', () => {
  require('./lib/integration')(this); // eslint-disable-line global-require, max-len

  before((done) => {
    done();
  });

  after((done) => {
    done();
  });
});
